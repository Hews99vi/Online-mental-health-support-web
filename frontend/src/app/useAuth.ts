/**
 * useAuth.ts
 *
 * Dedicated entry-point for the useAuth hook so it can be imported as:
 *   import { useAuth } from 'src/app/useAuth'
 *
 * Implementation lives in AuthContext.tsx.
 */
export { useAuth } from './AuthContext';
