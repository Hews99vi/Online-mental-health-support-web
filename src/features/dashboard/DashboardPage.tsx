/**
 * DashboardPage.tsx
 *
 * Entry-point for the /dashboard route — delegates immediately to the
 * role-aware router which redirects to the correct sub-dashboard.
 */
export { RoleBasedHome as DashboardPage } from './RoleBasedHome';
